{
  "name": "finedge",
  "version": "1.0.0",
  "description": "Finedge - Professional Full-Stack Trading Platform",
  "main": "index.js",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install && cd ../dashboard && npm install",
    "start-backend": "cd backend && npm start",
    "start-frontend": "cd frontend && npm start", 
    "start-dashboard": "cd dashboard && npm start",
    "dev": "concurrently \"npm run start-backend\" \"npm run start-frontend\" \"npm run start-dashboard\"",
    "build-all": "cd frontend && npm run build && cd ../dashboard && npm run build"
  },
  "keywords": [
    "trading",
    "finedge",
    "react",
    "nodejs",
    "mongodb",
    "portfolio",
    "stocks",
    "finance"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}